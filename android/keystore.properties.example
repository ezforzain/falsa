# Copy this file to keystore.properties (same folder) and fill in real values — that file is
# gitignored and must never be committed. See android/README.md "Create a release keystore".
storeFile=../release-keystore.jks
storePassword=CHANGE_ME
keyAlias=falsafahtot
keyPassword=CHANGE_ME
