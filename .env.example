# Only needed for a production build (e.g. Vercel) where the backend isn't reachable via
# Vite's dev-only proxy. Set this to wherever /server actually runs, with no trailing slash.
# Leave unset for local dev — requests fall back to relative /api/... paths as before.
VITE_API_URL=https://your-backend-host.example.com
